<?xml version="1.0" encoding="utf-8" ?>

<odoo>

    <!--
        Sale Order (sale)
    -->
    <record id="view_order_form" model="ir.ui.view">
        <field name="name">sale_order.form.inherited</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='payment_term_id']" position="after">
                <field name="event_id" readonly="1"/>
                <field name="event_attr_value_id" groups="base.group_no_one"/>
            </xpath>

            <xpath expr="//field[@name='order_line']/form//field[@name='product_id']" position="attributes">
                <attribute name="domain">[('sale_ok', '=', True), '|', ('company_id', '=', False), ('company_id', '=', parent.company_id),
                    ('product_template_attribute_value_ids.product_attribute_value_id', '=', parent.event_attr_value_id)]</attribute>
            </xpath>

            <xpath expr="//field[@name='order_line']/tree//field[@name='product_id']" position="attributes">
                <attribute name="domain">[('sale_ok', '=', True), '|', ('company_id', '=', False), ('company_id', '=', parent.company_id),
                    ('product_template_attribute_value_ids.product_attribute_value_id', '=', parent.event_attr_value_id)]</attribute>
            </xpath>

        </field>
    </record>



</odoo>
